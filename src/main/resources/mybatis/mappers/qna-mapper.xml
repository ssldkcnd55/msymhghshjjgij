<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna">

<select id="qnaList" parameterType="int" resultType="Market_qna">
	<![CDATA[
	select * from market_qna where market_no = #{market_no} order by market_qna_no desc
	]]>

</select>
	
	<select id="qnaCount" parameterType="int" resultType="int">
		select count(*) from market_qna where market_no = #{market_no}
	</select>
	
</mapper>
